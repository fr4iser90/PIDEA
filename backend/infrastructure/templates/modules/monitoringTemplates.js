/**
 * Monitoring Templates Module
 */
const { TEMPLATE_CATEGORIES } = require('./constants');

class MonitoringTemplates {
    /**
     * Get monitoring script templates
     * @returns {Object} Monitoring templates
     */
    static getTemplates() {
        return {
            // Placeholder for monitoring templates
            // Will be populated from original file
        };
    }
}

module.exports = MonitoringTemplates; 